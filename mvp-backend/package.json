{
  "name": "finbot-mvp-backend",
  "version": "1.0.0",
  "description": "Express proxy backend for FinBot MVP — routes LLM calls server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test utils/sanitizeMessages.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
